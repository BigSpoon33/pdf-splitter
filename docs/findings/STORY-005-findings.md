# Findings — STORY-005
**Date:** 2026-09-25
**Status:** done

## AC Verification
- [x] AC-1: `POST /api/jobs` (`src/pdf_splitter/upload.py:147` `create_job`, a sync route on the threadpool) mints the id first, creates `<jobs_dir>/<id>/` and copies the upload in 1 MiB chunks to `<id>/source.pdf.part` (`upload.py:98` `_copy_capped`). It gives up as soon as the running count passes `max_bytes` and returns 413 `too_large`. Contracts: `tests/test_upload.py::test_too_large`, `::test_too_large_stops_copying_at_the_cap` (the copy never reads more than one chunk past the cap), `::test_upload_exactly_at_the_cap_is_accepted`.
- [x] AC-2: The API checks `%PDF-` itself (`upload.py:125`) and then runs `[sys.executable, "-m", "pdf_splitter.preflight", "--max-pages", N, "--", path]` under a 10 s timeout (`upload.py:71` `run_preflight`, `PREFLIGHT_TIMEOUT = 10.0`; the `--` and the absolute `jobs_dir` are gate r1 fixes, see below). `src/pdf_splitter/preflight.py:29` `check()` works in AC order: magic → `not_pdf`; open fails → `unreadable`; `needs_pass` → `encrypted`; `page_count > max_pages` → `too_many_pages` (413); fewer than 50 non-whitespace chars over up to 12 evenly spaced pages (`probe_indices`, first and last page included) → `no_text_layer`. A timeout, a non-zero exit, unparseable stdout, a missing/invalid `pages` or an unknown `code` all become 400 `unreadable`. Contracts: `::test_preflight_check` (6 cases), `::test_preflight_order_encrypted_before_page_count`, `::test_preflight_text_probe_needs_50_chars_across_sampled_pages`, `::test_probe_indices_are_evenly_spaced_and_capped`, `::test_preflight_subprocess_prints_one_json_object`, `::test_preflight_uses_a_10s_timeout_and_this_interpreter`, `::test_unreadable_on_preflight_timeout`, `::test_unreadable_on_preflight_crash_or_garbage` (7 cases).
- [x] AC-3: On success `source.pdf.part` → `source.pdf`, then `store.create_job(job_id=…, filename=sanitize_filename(...), bytes, pages, ttl_hours=settings.ttl_hours, ip_hash=…)` with the defaults `queued`/`analyze`, and a 201 `{id, state}` response (`upload.py:129-140`). `sanitize_filename` (`upload.py:51`) keeps the basename after `/` or `\`, drops every Unicode `C*` character (controls, and format chars such as U+202E), strips, falls back to `document.pdf`, and caps at 120 characters (keeping a short extension). Contracts: `::test_upload_creates_queued_analyze_job`, `::test_sanitize_filename` (9 cases), `::test_sanitize_filename_caps_at_120_keeping_the_extension`, `::test_uploaded_long_traversal_name_is_sanitized`.
- [x] AC-4: `_accept` (`upload.py:110`) runs everything after `mkdir` inside `try/finally`, and `rmtree`s `<id>/` unless the row was inserted. That covers every rejection, exceptions and timeouts, including a failing `create_job`. Every rejection test asserts no `<id>/` directory, no row (`COUNT(*) FROM jobs == 0`), and no `jobs_dir` path in the body (`assert_rejected`). Also `::test_row_insert_failure_leaves_no_directory`.
- [x] AC-5: The fixtures are generated in the test with PyMuPDF: a text PDF, an AES-256 encrypted one, an image-only one (a pixmap, no text), real PNG bytes uploaded as `x.pdf`, a 4-page PDF against `max_pages=3`, `%PDF-` + garbage, and an empty or missing file.
- Orchestrator addendum (job ids never reach logs): `cli.py:24` runs uvicorn with `access_log=False`. `src/pdf_splitter/access_log.py` is an HTTP middleware (registered in `app.py:35`) that logs `METHOD path status ms` on logger `pdf_splitter.access`. `redact_path` hashes every 16+ run of the id alphabet anywhere in the path AND whatever sits in the segment after `/api/jobs/` (gate r2 rule, see below; gate r1 had stopped keying on the prefix alone), `loggable_path` percent-encodes the result, and the query string is dropped. Contracts: `::test_redact_path`, `::test_redact_path_hashes_an_id_glued_to_other_alphabet_chars`, `::test_redact_path_cannot_be_dodged_by_path_shape`, `::test_logged_path_is_escaped`, `::test_logs_never_contain_a_job_id`, and `tests/test_health.py::test_cli_api_serves_the_app_from_env_settings` (asserts `access_log is False`). Concurrency: `::test_concurrent_uploads_all_succeed` runs 6 threads × 3 uploads behind a `Barrier` with `raise_server_exceptions=True` and gets 18 distinct ids, rows and dirs.

## Test Results
**Command:** `uv run pytest && uv run ruff check`
**Result:** pass
```
118 passed in 4.74s
All checks passed!
```
Before: 23. After the story (`b84c3c4`): 67 (44 in `tests/test_upload.py` counting parametrized cases; the existing CLI test gained an `access_log is False` assertion). After the gate r1 fix (`4006eff`): 86. After the gate r2 fix (`676dbbf`): 118 (32 glued/truncated-id cases, and the path-shape test now asserts the exact redacted string).

Manual run on port 8010 (8000 is held by an unrelated process), `PDFSPLIT_JOBS_DIR` pointed at a scratch dir, server stopped by PID:
```
curl -F "file=@book.pdf;filename=../../My Book.pdf"   → {"id":"eWyFfdQExP_jq1A246LCsQ","state":"queued"} 201
curl -F file=@blank.pdf                               → {"code":"no_text_layer","message":"The PDF has no text layer (it looks scanned); run OCR on it first."} 400
curl -F "file=@x.png;filename=x.pdf"                  → {"code":"not_pdf","message":"The file is not a PDF."} 400
(PDFSPLIT_MAX_BYTES=1000) curl -F file=@book.pdf      → {"code":"too_large","message":"The file is larger than the upload limit."} 413
jobs/ after: jobs.db + <id>/source.pdf only; row: queued|analyze|My Book.pdf|2500|5
server log: "POST /api/jobs 201 125.0ms", "GET /api/jobs/45ebecb8 404 0.3ms"; raw id occurrences in the log: 0
```

## Gate r1 fixes (`4006eff`, review: `docs/findings/STORY-005-review.md`)
Three confirmed findings, fixed forward in one commit. Review finding 2 (500s carry a request id) was refuted as a STORY-005 defect and is STORY-007's AC-7; nothing for it was built here.

1. **Redaction that can't be dodged** (finding 1). `access_log.redact_path` (`access_log.py:25`) no longer anchors on `^/api/jobs/`. It replaces every token shaped like `new_job_id()` output, `[A-Za-z0-9_-]{22}` bounded by non-token characters, wherever it sits in the path, so `//api/jobs/<id>`, `/api/jobs//<id>`, `/api//jobs/<id>`, `/API/jobs/<id>`, `/api/jobs/./<id>`, `/api/jobs/../jobs/<id>`, the absolute form `http://h:1/api/jobs/<id>`, `<id>.json` and `<id>;v=1` all log `log_id(id)`. Deliberately a token match, not a segment match, so an extension or a `;` parameter glued to the id can't hide it either; 21- or 23-char runs and runs with other characters are left alone (the SPA's hashed asset names contain a `.`). The middleware reads `request.scope["path"]` (decoded once, the string the router matched on) rather than `request.url.path`, which decodes a second time. Contracts: `tests/test_upload.py::test_redact_path`, `::test_redact_path_cannot_be_dodged_by_path_shape` (12 shapes × a fixed dash-leading id and a fresh `new_job_id()`).
2. **Log paths escaped** (finding 3). `access_log.loggable_path` (`access_log.py:29`) percent-encodes the redacted path with `urllib.parse.quote(..., safe="/:@!$&'()*+,;=-._~")` (the RFC 3986 path characters). The ASGI path is percent-decoded client input, so `%1B%5B31m` had become a real ESC and `%E2%80%A8` a real U+2028 by the time it was logged; they are now logged as `%1B%5B31m` and `%E2%80%A8`, a literal `%` as `%25`, and every access line is ASCII and printable. Contract: `::test_logged_path_is_escaped` (through the TestClient, which delivers the decoded bytes exactly like uvicorn).
3. **Preflight argv hardened** (finding 4). `Settings.jobs_dir` has an after-validator that `resolve()`s it (`config.py:27`), so `PDFSPLIT_JOBS_DIR=.` or empty gives `Path.cwd()`, and `db_path` is absolute too; the default `jobs` therefore reads back as `<cwd>/jobs` (`tests/test_config.py::test_defaults` was updated). Independently, `run_preflight` puts `--` before the path (`upload.py:74`), so an id starting with `-` (1 in 64) can never parse as an option even if a future caller passes a relative path. Contracts: `tests/test_config.py::test_jobs_dir_is_always_absolute`, `tests/test_upload.py::test_upload_with_relative_jobs_dir_and_dash_id` (chdir to tmp, `jobs_dir=Path(".")`, forced ids `-bCd…` and `--Cd…` → 201 + `source.pdf` + rows), `::test_preflight_main_accepts_a_dash_leading_path` (and shows argparse rejects it without `--`), and `::test_preflight_uses_a_10s_timeout_and_this_interpreter` now asserts `cmd[-2] == "--"` and an absolute `cmd[-1]`.

Re-verified live on port 8012 (server stopped by PID): the reviewer's seven path shapes plus `%1B%5B31m`, `%E2%80%A8%C2%85<id>%0A` and `%25`, sent as raw HTTP request targets, all logged `GET … /23962981 404` (the hash of the dash id) with zero raw ids, zero ESC bytes and zero non-ASCII bytes in the log; the reviewer's `dash.py` now prints `dash-id 201` and `normal-id 201`.

## Gate r2 fix (`676dbbf`, review § Round 2, Shuma-approved rule)
The r1 regex matched only runs of EXACTLY 22 id-alphabet chars bounded by non-alphabet chars, so an id touching any other `[A-Za-z0-9_-]` char (`<id>x`, `x<id>`, `<id><id>`, `<id>-extra`, `<id>_`, `<id>%41` → `<id>A`) or truncated to 21 chars was logged raw, and `tests/test_upload.py:403-405` asserted exactly that. Replaced by two rules in `access_log.redact_path` (`access_log.py:23-38`), applied in this order and then escaped by `loggable_path` as before:

1. **Slot rule** (`_JOBS_SLOT`, the parent's rule kept as defence in depth): after a case-insensitive `/api/jobs/` with any number of repeated slashes, the entire next `[^/]+` segment is replaced by `log_id(segment)` whatever its length, so a 2-char fragment, `.`/`..`, an ESC sequence or `<id>.json` in the id slot are all hashed as one unit. Consequence worth knowing: `/api/jobs/<id>.json` logs `log_id("<id>.json")`, not `log_id(id) + ".json"` (the path-shape test pins the exact strings).
2. **Run rule** (`_ID_RUN`): every maximal run of `[A-Za-z0-9_-]` of length **≥ 16**, anywhere in the string, is replaced by `log_id(run)`. Greedy `{16,}` can only match a whole run, never a slice, so an id glued to anything in the alphabet is hashed together with it, and any truncation down to 16 chars is hashed too (a 15-char fragment outside the slot is by design not a credential). The slot rule runs first because its output is an 8-hex hash the run rule leaves alone; the reverse order would log a hash of a hash for every real id.

Route words are unaffected: `health`, `sheets`, `sections`, `result.zip`, `plan`, `cut` are all shorter than 16 and never sit in the id slot. Anything else 16+ alphabet chars long in a path (a long hashed SPA asset name without a `.`) will be hashed in the log; that is log-only and acceptable. Contracts: `::test_redact_path` (route words, the 15/16 boundary, the slot rule on short garbage), `::test_redact_path_hashes_an_id_glued_to_other_alphabet_chars` (8 glue/truncation variants × 4 placements × a fixed and a fresh id: output changed, no id, no 16-char window of the id), `::test_redact_path_cannot_be_dodged_by_path_shape` (the 12 shapes, now with the exact expected string), `::test_logged_path_is_escaped` (5 requests; the ESC and U+2028/U+0085 cases moved out of the id slot so the escaping is still exercised, plus the slot-hashed ESC and the `%41` glue through a real request). Mutation-checked: the r1 regex fails 38 of these 47 selected tests.

Re-verified live on port 8014 (server stopped by PID, `PDFSPLIT_JOBS_DIR` in a scratch dir): a real upload's id sent in 28 raw request targets (the 6 glue variants, 21/16-char truncations, three placements outside the slot, the seven r1 shapes, `.json`, the ESC and separator cases, and the five route words) produced 31 access lines with 0 raw ids, 0 sixteen-char windows of the id, 0 non-ASCII/control lines, 0 ESC bytes, and every route word still legible (e.g. `GET /api/jobs/79803fe2/result.zip 404`).

## Bugs Found
- **PyMuPDF 1.28.2 prints a `fitz` deprecation notice to STDOUT** (`warning: The \`fitz\` API is deprecated…`) when anything runs `import fitz`. That broke a "stdout is one JSON object" contract on the first run. Fixed two ways: `preflight.py` imports `pymupdf`, and `run_preflight` parses only the last stdout line. The kickoff's `import fitz` advice is outdated for this pin, and any subprocess protocol in STORY-006 should use the same last-line rule.

## Decisions (small ambiguities resolved)
- **Error body:** `{"code": str, "message": str}` (`upload.py:67` `reject`, messages in `upload.MESSAGES`). 413 for `too_large`/`too_many_pages`, and 400 for everything else. The body never includes a path or the id. `tests/test_upload.py::assert_rejected` is the contract.
- **`unreadable` is a new code.** Architecture § api/§ Interfaces list `not_pdf|encrypted|no_text_layer` for 400; the story adds `unreadable` (a crash or timeout). Architecture should add it (doc gap, not changed here).
- **Missing or empty file:** 400 `not_pdf` instead of FastAPI's 422 `{"detail"}`, so every upload rejection has the same shape.
- **Size cap timing:** FastAPI/Starlette parse the multipart body (spooling to a temp file after 1 MB) *before* the handler runs, so `too_large` is decided while copying from that spool, not while the bytes arrive. Memory use stays bounded (the story accepts this); the 210 MB network cap is Caddy's (STORY-013). A real early abort would need a raw `request.stream()` parser, which is only worth building if disk-spool abuse shows up.
- **`--max-pages` is passed to the preflight**, so a 50 000-page PDF is refused before the text probe.
- **Magic bytes are checked twice**: in the API (skips a spawn for obvious junk) and in `preflight.check` (so the module is safe on its own).
- **`ip_hash`** = `sha256(per-process random salt + request.client.host)` (`upload.py:47`). It stores no raw IP and ignores `X-Forwarded-For`. STORY-012 replaces it.
- **`app.py` → `deps.py`:** `get_settings`/`get_store`/`SettingsDep`/`StoreDep` moved to `src/pdf_splitter/deps.py`, because `upload.py` needs them and `app.py` imports `upload.py`. `app.py` re-exports them.
- **`job_dir.mkdir()` is outside the `try`**, so a (theoretical) id collision raises instead of letting the cleanup delete another job's directory.
- Preflight stderr is never logged, because MuPDF messages can quote the path (which contains the id). Only `log_id` + the exit code are logged.

## Handoff Context for Next Session
A successful upload leaves exactly `<jobs_dir>/<id>/source.pdf` and one row `state='queued', kind='analyze'` with `pages`/`bytes`/sanitized `filename`. `upload.run_preflight` is the pattern for the worker's subprocess runner: `sys.executable -m <module>`, options first, then **`--` before any positional that can start with `-`** (job ids and the paths built from them can), `capture_output`, `stdin=DEVNULL`, `timeout`, the result on the LAST stdout line, anything else collapsed to one error code, and only `log_id` logged. `settings.jobs_dir` is always absolute now, so paths derived from it are safe to hand to argv, but keep the `--` regardless. Any string the worker logs that could carry an id (argv, a path, a stderr tail) goes through `access_log.redact_path` first: its run rule hashes every 16+ run of `[A-Za-z0-9_-]` in ANY string, so it is safe on command lines too, while the slot rule only fires on `/api/jobs/` paths. The review also noted (not a finding here) that preflight has no memory rlimit: a 2.5 KB nested-XObject PDF reached ~790 MB RSS inside the 10 s window, so STORY-006 should apply the worker's rlimits to the preflight launch as well.

## Out-of-Scope Items
- Rate limiting, the trusted-proxy IP rule and the disk-full 503 (STORY-012). The early-abort streaming parser (see Decisions). Caddy's body cap (STORY-013).
- Doc gap: Architecture § api / § Interfaces don't list `unreadable` (400) as a `POST /api/jobs` code.
